import React from 'react';
import classes from './scrollpage.module.css';
const ProfileImage = (props) => <section className={classes.ProfileImage}>{props.children}</section>;

export default ProfileImage;
